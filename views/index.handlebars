<div class="container-fullwidth">
    <div class="row">
        <div class="col-md-12">
            <nav class="navbar navbar-expand-lg navbar-light bg-secondary">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
    <a class="navbar-brand" href="#">Get Scraped</a>
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item">
        <a class="nav-link" href="/saved">Saved</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"><button id="buttScrape" class="btn btn-success">Scrape</a></button>
      </li>
     
    </ul>
    
  </div>
</nav>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="jumbotron jumbotron-fluid jumbo">
                <div class="container">
                    <h1 class="display-4">Fluid jumbotron</h1>
                    <p class="lead">This is a modified jumbotron that occupies the entire horizontal space of its parent.</p>
                </div>
            </div>
        </div>
    </div>
    {{!-- end jumbotron --}}
    <div class="row">
        <div class="col-md-12">
        {{#if articles}}    
            {{#each articles}}
            <br>
                <div class="Card cardSt">
                    <div class="card-header">
                        <h3 class="art-title">{{this.title}}
                        <button type="button" class="btn btn-success sv" data-id="{{this._id}}">save</button>
                        <button type="button" class="btn btn-secondary notey">Notes</button>
                        
                        </h3>
                    </div>
                    
                    <div class="card-body">
                        <a href={{link}}>{{this.link}}</a>
                        <p class="card-text">{{this.summary}}</p>
                    </div>
                </div>
            <br>
            {{/each}}
        {{/if}}
        </div>
    </div>
    <div class="row">
        <div>
            
        </div>
    </div>
</div>