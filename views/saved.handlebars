<div class="container-fullwidth">
    <div class="row">
        <div class="col-md-12">
            <nav class="navbar navbar-expand-lg navbar-light bg-secondary">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
    <a class="navbar-brand" href="#">Get Scraped</a>
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item">
        <a class="nav-link" href="/">Articles</a>
      </li>
      <li class="nav-item">
        <a class="nav-link"><button id="buttScrape" class="btn btn-success">Scrape</a></button>
      </li>
     
     
    </ul>
    
  </div>
</nav>
        </div>
    </div>
    
    <div class="row">
        <div class="col-md-12">
        {{#if articles}}    
            {{#each articles}}
            <br>
                <div class="Card cardSt">
                    <div class="card-header">
                        <h3 class="art-title">{{this.title}}
                        {{!-- <button type="button" class="btn btn-success sv">save</button> --}}
                        <a href="/addnote/{{this._id}}"><button type="button" class="btn btn-secondary notey" data-id="{{this._id}}">Notes</button></a>
                        
                        </h3>
                    </div>
                    
                    <div class="card-body">
                        <a href={{link}}>{{this.link}}</a>
                        <p class="card-text">{{this.summary}}</p>
                    </div>
                </div>
            <br>
            {{/each}}
        {{/if}}
        </div>
    </div>
    <div class="row">
        <div>
            
        </div>
    </div>
</div>